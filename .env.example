# Base URL of the Strapi backend (see cms/.env.example for its port/host).
STRAPI_URL=http://localhost:1337

# Shared secret for the Next.js -> Strapi revalidation webhook. Must match
# REVALIDATE_SECRET in cms/.env exactly.
REVALIDATE_SECRET=
